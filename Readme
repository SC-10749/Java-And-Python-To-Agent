1.String StringBuffer StringBuilder的区别
  ● String底层被 final 修饰的字符数组实现，不可变；每次拼接、修改都会生成新字符串对象，频繁操作大量产生垃圾，效率最低。
  ● StringBuffer方法带有 synchronized 同步锁，线程安全；多线程场景使用，有锁竞争开销，效率中等。
  ● StringBuilder无 synchronized、线程不安全；无同步开销，效率最高；二者初始默认容量都为 16。

  2.为什么重写equals方法的同时一定要重写hashCode方法
  1.因为 Java 规范规定，equals 相等的对象 hashCode 必须相同。
  2.两个hashcode相等的数，equals可以相等或者是不相等
  hashcode规则(重点)
  如果Object对象的hashCode()方法没有重写，那么Object对象的hashCode方法有如下规则：
  1.  同一对象的hashCode值相同
  2. 同一个类的两个对象无论如何都不会相等（即使这两个对象具有相同的数据）
   哈希集合（HashMap/HashSet）先算 hashCode 定位，再用 equals 比较
   不一起重写 → 违反契约 → 集合功能失效